<UserControl x:Class="Szpital.Components.BindableLoginPasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Szpital.Components"
             mc:Ignorable="d" >
    <PasswordBox
        MaxLength="20"
        FontSize="13"
        FontWeight="Medium"
        FontFamily="{StaticResource Nunito}"
        Foreground="White"
        CaretBrush="LightGray"
        BorderBrush="DarkGray"
        BorderThickness="0 0 0 1"
        Padding="20 0 0 0"
        VerticalContentAlignment="Center" x:Name="passwordBox" PasswordChanged="PasswordBox_PasswordChanged">
        <PasswordBox.Background>
            <ImageBrush ImageSource="../Images/padlock.png" Stretch="Uniform"
                AlignmentX="Left"/>
        </PasswordBox.Background>
    </PasswordBox>
</UserControl>
